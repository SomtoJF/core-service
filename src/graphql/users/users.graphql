type User {
  id: ID!

  email: String!
  hash: String!
  firstName: String!
  lastName: String!
  profilePicture: String
  role: String!
  examsTaken: [String!]
  examsSet: [String!]
  #   createdAt:
  #   updatedAt:
}

type Query {
  user(id: ID!): User
}
